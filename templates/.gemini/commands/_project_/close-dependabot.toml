description = "Close a Dependabot security alert with a documented reason"
prompt = """
Close Dependabot security alert #{{args}}.

Steps:

1. **Fetch alert**:
   - `gh api repos/:owner/:repo/dependabot/alerts/{{args}}`.
   - If already closed, inform user and exit.

2. **Interact / Execute**:
   - **If user has NOT provided a reason**:
     - Display alert details.
     - Ask for close reason (false positive, not exploitable, mitigated, etc.).
     - Require detailed explanation (minimum 20 characters).
     - **Stop and wait for user response.**

   - **If user has provided a reason** (in context or arguments):
     - Confirm reason is reasonable.
     - Execute close:
       ```bash
       gh api --method PATCH "repos/:owner/:repo/dependabot/alerts/{{args}}" \
         -f state=dismissed -f dismissed_reason="<reason>" -f dismissed_comment="<comment>"
       ```
     - Record in associated task (if any).
     - Inform user of result.

3. **Next step**:
   If other pending security alerts remain:
   - Claude Code / OpenCode: `/analyze-dependabot {alert-number}`
   - Gemini CLI: `/{project}:analyze-dependabot {alert-number}`
   - Codex CLI: `/prompts:{project}-analyze-dependabot {alert-number}`

**Note**: Exercise caution when closing high-severity alerts. Prefer fixing over dismissing.
"""
