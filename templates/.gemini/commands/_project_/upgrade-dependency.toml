description = "Upgrade a dependency package to a new version"
prompt = """
Upgrade a dependency package. Arguments: <package-name> <old-version> <new-version>

{{args}}

Steps:

<!-- TODO: Adapt to your project's package manager -->

1. **Find the target package** in the project's dependency file.
   ```bash
   # TODO: Replace with your dependency file
   # Example: package.json / pom.xml / requirements.txt / go.mod / Cargo.toml
   ```

2. **Update the version number**.

3. **Install the new version**:
   ```bash
   # TODO: Replace with your project's install command
   # Example: npm install / mvn clean install / pip install -r requirements.txt / go mod tidy
   ```

4. **Verify build**:
   ```bash
   # TODO: Replace with your project's build command
   # Example: npm run build / mvn compile / make build / go build ./...
   ```

5. **Run tests** to ensure nothing is broken.

6. **Output change summary**.
   - Prompt user to commit:
     - Claude Code / OpenCode: `/commit`
     - Gemini CLI: `/{project}:commit`
     - Codex CLI: `/prompts:{project}-commit`

**Note**: For major version upgrades, warn about potential breaking changes. Do NOT auto-commit.
"""
