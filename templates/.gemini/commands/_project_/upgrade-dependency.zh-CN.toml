description = "升级项目依赖到新版本"
prompt = """
升级项目依赖。参数格式: <package-name> <old-version> <new-version>

{{args}}

执行步骤:

<!-- TODO: 适配你的项目的包管理器 -->

1. **查找目标包**:
   ```bash
   # TODO: 替换为你的依赖文件
   # 示例: package.json / pom.xml / requirements.txt / go.mod / Cargo.toml
   ```

2. **更新版本号**。

3. **安装新版本**:
   ```bash
   # TODO: 替换为你的项目的安装命令
   # 示例: npm install / mvn clean install / pip install -r requirements.txt / go mod tidy
   ```

4. **验证构建**:
   ```bash
   # TODO: 替换为你的项目的构建命令
   # 示例: npm run build / mvn compile / make build / go build ./...
   ```

5. **运行测试**，确保没有破坏。

6. **输出变更摘要**。
   - 提醒用户提交:
     - Claude Code / OpenCode: `/commit`
     - Gemini CLI: `/{project}:commit`
     - Codex CLI: `/prompts:{project}-commit`

**注意**: 如果是 major 版本升级，提示可能的 breaking changes。不要自动提交代码。
"""
