description = "审查技术方案"
prompt = """
审查任务 {{args}} 的技术方案。

任务 scope 可省略，或通过 `--task <ref>` / `-t <ref>` 显式覆盖；`--pr` 等专用锚点保持原语义。

读取并执行 `.agents/skills/review-plan/SKILL.md` 中的 review-plan 技能。

严格按照技能中定义的所有步骤执行。
"""
